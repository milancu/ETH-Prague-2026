ALCHEMY_API_KEY=your_alchemy_key_here
ETHERSCAN_V2_API_KEY=your_etherscan_key_here

# Address of the wallet that will resolve markets (oracle role).
# On local Hardhat this defaults to the deployer (account[0]).
# On testnets/mainnet set this to a wallet YOU control — never a publicly known key.
# For production: use a Gnosis Safe multisig address.
ORACLE_ADDRESS=